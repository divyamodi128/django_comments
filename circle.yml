machine:
  python:
    version: 3.4.4
dependencies:
  pre:
    - pip install -r requirements.txt
script: "cd src"
test:
  pre:
    - python src/manage.py test
deployment:
  production:
    branch: master
    commands:
      - fab deploy
